 {{#each message as |ms|}}

<article class="list-group-item list-group-item-action flex-column align-items-start">
  <div class="d-flex w-100 justify-content-between">
    <h5 class="mb-1">Mensaje</h5>
    <small id="time">{{ms.createdAt}}</small>
  </div>
  <p class="mb-1">{{ms.body}}</p>
  <small>{{ms.to}}</small>
</article>
{{/each}}

<script>
  let timeTag = document.getElementById("time").innerHTML;
  console.log(timeTag)

</script>