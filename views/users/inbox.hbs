<main class="container">

  <a href="/user/{{user.name}}" class="btn btn-primary">Back to profile</a>

  THE SENDERS
  {{!-- {{user}} --}}
  <section class="inbox">
  {{#each senders as |sender|}}

    <article>
      <a href="/user/{{../user.name}}/messages/{{sender}}" class="list-group-item list-group-item-action flex-column align-items-start">
        <div class="d-flex w-100 justify-content-between">
          <h5 class="mb-1">Mensaje</h5>
        </div>
        <p class="mb-1">{{sender}}</p>
        <a href=""></a>
    </a>
    </article>
  {{/each}}
  </section>

</main>

